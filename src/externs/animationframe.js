/**
 * @type {?function(function(), ?Element)}
 */
Window.prototype.requestAnimationFrame;

/**
 * @type {?function(number)}
 */
Window.prototype.cancelAnimationFrame;

/**
 * @type {?function(function(), ?Element)}
 */
Window.prototype.webkitRequestAnimationFrame;

/**
 * @type {?function(number)}
 */
Window.prototype.webkitCancelRequestAnimationFrame;

/**
 * @type {?function(function(), ?Element)}
 */
Window.prototype.mozRequestAnimationFrame;

/**
 * @type {?function(number)}
 */
Window.prototype.mozCancelRequestAnimationFrame;

/**
 * @type {?function(function(), ?Element)}
 */
Window.prototype.msRequestAnimationFrame;

/**
 * @type {?function(number)}
 */
Window.prototype.msCancelRequestAnimationFrame;

/**
 * @type {?function(function(), ?Element)}
 */
Window.prototype.oRequestAnimationFrame;

/**
 * @type {?function(number)}
 */
Window.prototype.oCancelRequestAnimationFrame;
